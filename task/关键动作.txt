关联review_queue.csv与risk_login_events.json，确认每个队列项都能定位到风险事件
读取freeze_policy.json与admin_roles.json，按裁决顺序、阈值、信号和角色权限推导页面处置
使用Playwright在导航前提供本地接口响应并固定浏览器业务时间，核对账号卡片顺序、徽标和按钮状态
根据策略参数计算TOTP接受窗口，将当前时间步验证码用于具备冻结资格的页面提交
通过Tab与Enter走完主冻结流程，记录实际获得焦点的页头控件、冻结按钮和对话框控件
捕获冻结请求并核对方法、地址、字段、会话和幂等键，确认受限账号未产生请求
整理裁决矩阵、TOTP窗口、请求回执和焦点路径，使四份报告可由事件与策略交叉复算
