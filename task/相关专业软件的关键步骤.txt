1. 读取队列、风险事件、角色权限和冻结策略，建立queue_item_id、event_id与account_id之间的关联
2. 配置Playwright从运行环境读取输入根目录、输出根目录和本地端口，并使用策略指定的Chromium项目、时区与服务启动预算
3. 在页面导航前路由风险事件与冻结策略接口，并通过addInitScript固定浏览器业务时间
4. 打开控制台，按策略裁决顺序检查每张账号卡片的排序、徽标和冻结按钮状态
5. 从TOTP配置读取密钥、算法、位数、步长和偏移，计算接受窗口并选择当前时间步验证码
6. 使用Tab与Enter打开主账号冻结对话框，填写原因和验证码后提交，同时记录每个焦点节点
7. 对其余可冻结账号完成页面提交，捕获请求方法、路径和正文，核对角色会话及幂等键
8. 从本次浏览器观察生成四份CSV报告，按队列项、事件和账号检查跨报告一致性
